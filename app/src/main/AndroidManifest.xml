<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".PTODAApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MBPTODABookingApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Google Maps API Key — replace YOUR_MAPS_API_KEY before testing maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD4we-oa3wABCTt2iKZEyOnzoF-5pfb6mk" />

        <!-- Launcher / Router -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".ui.auth.LoginActivity" android:exported="false" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="false" />

        <!-- Passenger -->
        <activity android:name=".ui.passenger.PassengerHomeActivity" android:exported="false" />
        <activity android:name=".ui.passenger.BookRideActivity" android:exported="false" />
        <activity android:name=".ui.passenger.RideStatusActivity" android:exported="false" />

        <!-- Driver -->
        <activity android:name=".ui.driver.DriverHomeActivity" android:exported="false" />
        <activity android:name=".ui.driver.RideRequestActivity" android:exported="false" />
        <activity android:name=".ui.driver.ActiveRideActivity" android:exported="false" />

        <!-- Admin -->
        <activity android:name=".ui.admin.AdminDashboardActivity" android:exported="false" />
        <activity android:name=".ui.admin.ManageUsersActivity" android:exported="false" />

        <!-- FCM -->
        <service
            android:name=".services.PTODAFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>